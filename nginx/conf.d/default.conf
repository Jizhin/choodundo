# HTTP server. For production, terminate TLS here (see commented 443 block).
server {
    listen 80;
    server_name _;

    client_max_body_size 1m;

    # --- API ---
    location /api/ {
        proxy_pass http://choodundo_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
    }

    # --- Health passthrough ---
    location = /health {
        proxy_pass http://choodundo_api/health;
        proxy_set_header Host $host;
    }

    # --- WebSocket ---
    location /ws/ {
        proxy_pass http://choodundo_api;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # --- Frontend (static SPA) ---
    location / {
        proxy_pass http://choodundo_web;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# --- Production TLS (uncomment & provide certs to enable HTTPS) ---
# server {
#     listen 443 ssl http2;
#     server_name choodundo.app;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#
#     # ... duplicate the location blocks above ...
# }
#
# server {
#     listen 80;
#     server_name choodundo.app;
#     return 301 https://$host$request_uri;
# }
